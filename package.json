{
	"name": "@nan0web/monorepo",
	"version": "1.0.0",
	"description": "Monorepo for @nan0web packages and applications",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"test": "node --test src/**/*.test.js",
		"test:docs": "node --test src/README.md.test.js",
		"test:release": "node --test releases/1/0/v1.0.0/**/*.test.js | grep #TODO",
		"dev:setup": "pnpm install && pnpm -r install",
		"dev:all": "pnpm -r run dev",
		"build:all": "pnpm -r run build",
		"docs:gen": "node scripts/gen-docs.js",
		"release:check": "node scripts/release-status.js",
		"release:check-app": "node scripts/release-status.js --app",
		"clean": "rm -rf node_modules && pnpm -r exec rm -rf node_modules",
		"status": "node bin/packages-status.js",
		"precommit": "npm test",
		"prepush": "npm test",
		"prepare": "husky"
	},
	"keywords": [],
	"author": "ЯRаСлав YaRaSLove <support@yaro.page>",
	"license": "ISC",
	"packageManager": "pnpm@10.11.0",
	"pnpm": {
		"overrides": {
			"@nan0web/*": "workspace:*"
		}
	},
	"devDependencies": {
		"@nan0web/co": "link:packages/co",
		"@nan0web/db": "link:packages/db",
		"@nan0web/db-fs": "link:packages/db-fs",
		"@nan0web/log": "link:packages/log",
		"@nan0web/test": "link:packages/test",
		"@nan0web/types": "link:packages/types",
		"husky": "^9.1.7"
	}
}
