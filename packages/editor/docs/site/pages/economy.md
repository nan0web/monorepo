# Economy & Themes

The Editor package natively supports theming through standard CSS custom properties.

## Publishing Your Theme

To submit a theme to the **Master IDE Theme Registry**:

1. Open the [Config Builder Sandbox](../sandbox/index.html).
2. Tweak `--co`, `--ba-surface` and other CSS variables.
3. Click "Export to Theme Store" (marketplace fee 18%).
