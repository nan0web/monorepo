#!/usr/bin/env node
/**
 * @function sync
 * @description Syncs translations from src/ into data/[locale]/t.yaml for the default locale.
 * @param {Object} args
 * @returns {Promise<void>}
 */
export default function sync(args?: any): Promise<void>;
