/**
 * VisualAdapter (React-Bootstrap)
 *
 * Рендерить інтенції у форматі Bootstrap-структур (JSON/HTML).
 */
export class VisualAdapter {
    /**
     * Конвертує інтенцію у візуальний блок Bootstrap.
     */
    static render(intent: any, t?: (k: any) => any): any;
}
