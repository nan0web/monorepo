import { TableModel } from '../../domain/view/TableModel.js';
export { TableModel };
/**
 * Interactive data table with filtering.
 * @param {Object} props - Table properties (data, columns, etc).
 */
export declare function Table(props: any): {
    $$typeof: symbol;
    type: string;
    props: any;
    model: any;
    execute: () => any;
};
