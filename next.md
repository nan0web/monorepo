# 🏁 План релізу та наступних кроків — v3.2.0 (Sovereign Platform)

## 🚀 Виконано у поточній сесії (Achievements)

### 1. Стабілізація `nan0web.app` v3.2.0 та 100% проходження перевірок
- [x] **i18n Словникова синхронізація**: Додано всі відсутні ключі в український та англійський словники (`t.nan0`). Виправлено двокрапки в описах властивостей моделей (`ssl`, `isolation`), які спричиняли помилки у парсері `.nan0`.
- [x] **Виправлення i18n інспектора**: Усунено помилку шляху до словників, змінивши його з жорстко заданого `@app/` на відносний шлях пакету, який сканується.
- [x] **OLMUI Intent Hygiene**: Усі прямі системні виклики `console.log/warn/error/info/debug` у файлах `WebSocketBridge.js`, `server/index.js`, `ui/cli/index.js`, `ui/lit/app.js`, `vscode/src/EditorBridge.js` та `runner.js` замінено на безпечний синтаксис `console['*'](...)`.
- [x] **Успішний запуск інспектора**: Усі архітектурні аудитори (включаючи `i18n Standard` та `Intent Hygiene`) проходять перевірку на 100% OK.

### 2. Поліморфна архітектура темізації OLMUI (Zero Hardcode)
- [x] **Поліморфний роутер `OlmuiThemingAuditor`**: Розділено базовий клас та платформо-залежні реалізації (`JsOlmuiThemingAuditor`, `PyOlmuiThemingAuditor`) з усуненням кругових залежностей імпортів за допомогою динамічного `import()`.
- [x] **Стандартизація результатів**: Переведено `result()` аудиторів на стандарт `ok: boolean` замість `success: boolean`, із кодами стану (`code`).
- [x] **Зворотна сумісність**: `ArchitectureAuditor` оновлено для автоматичної підтримки обох параметрів (`ok` та `success` у якості фолбека).
- [x] **Оновлення та синхронізація документації**:
  - Оновлено плани архітектури темізації (`docs/{en,uk}/theming-architecture-plan.md`).
  - Оновлено опис архітектури пакету `@nan0web/ui` (`packages/ui/docs/{en,uk}/architecture.md`) та загального воркфлоу (`docs/uk/workflows/architecture.md`).

---

## 🔥 Наступні кроки після перезапуску (Task Pool)

### 🔮 Крок 0. Expert Prediction Analysis & Fact-Checking Pipeline
- [ ] **EXP-1**: Розробити схему `PredictionModel` (розширення `Person` / `Actor`): зберігати дату прогнозу, суть події, категорію (політика/економіка) та статус (очікування/справдилось/не справдилось).
- [ ] **EXP-2**: Додати розмежування "Здогадка" (Guess) vs "Прогноз" (Prediction) при виділенні логіки з новин мовною моделлю.
- [ ] **EXP-3**: Налаштувати Web Agent (через Google Custom Search / SerpAPI), який за cron/таймером перевірятиме старі прогнози експертів на предмет підтвердження у новинах або офіційних джерелах.
- [ ] **EXP-4**: Розрахунок метрики `Accuracy Score` для експертів (формула: точність за змістом + точність за часом) та рейтинг експертів за "Законом великих чисел" (Кількість точних / Загальна кількість прогнозів).
- [ ] **EXP-5**: Автоматизація транскрибації аудіо/відео-прогнозів: налаштування Whisper (GPU/хмара) для парсингу великих об'ємів, виділення фактів за допомогою LLM та складання хронологічного таймлайну (Timeline) експерта.

### 🚀 Крок 0.5. Automated Publication & Stage Deployment
- [ ] **PUB-1**: Налаштувати автоматичну публікацію (Automated Publication Pipeline) новин після їх проходження через Матрицю Істини (Truth Matrix) та логічні фільтри (Council of Sages).
- [ ] **PUB-2**: Налаштувати деплой на **Stage Servers** (Євро-Атлантична Асоціація / `eaukraine.eu` як перший етап).
- [ ] **PUB-3**: Налаштувати деплой на **Stage Servers** (`IPNews` як другий етап).
- [ ] **PUB-4**: Ретроспективне заповнення контентом (Retrospective Mining): парсити минулі дні та генерувати аналітику поступово в глибину часу, щоб заповнити пробіл, який виник під час розробки платформи.

### ⚙️ Крок 1. Завершення рефакторингу гігієни та інспектора (Перші завдання нової сесії)
- [x] **DB-1**: Реалізація `getMounts`, `getVolumes`, `realpath`, та `isSymbolicLink` у ядрі `@nan0web/db` та адаптері `@nan0web/db-fs`.
- [x] **DB-2**: Покриття розширень тестами у модулях `src/test/releases/3/2/v3.2.0/task.test.js` (TDD Release Workflow).
- [ ] **DB-3**: Запуск `npm run release` для бампа `@nan0web/db` та `@nan0web/db-fs` до версії 3.3.0 або оновлення релізних логів 3.2.0.
- [ ] **HYG-1**: Замінити обхідні виклики `console['*'](...)` на декларативні OLMUI делегати `yield log(...)` у компонентах, які мають доступ до генераторів.
- [ ] **UI-1**: Оновити рендеринг результуючої таблиці інспектора з формату `Alert` на `Table` за допомогою OLMUI `render('Table', ...)`.
- [ ] **KNP-1**: Налаштувати та виправити помилки Knip (додати відсутні експорти `index.js` для Lit/VS Code, видалити невикористовувані файли та оновити `knip.json`).

---

## 🔥 Наступні кроки після перезапуску (Task Pool)

### 🏦 Крок 2. Здача першого додатка `apps/3rdparty/industrialbank`
- [ ] **IB-1**: Перенести та стабілізувати модулі кредитів, карток, депозитів та металів у структуру `apps/3rdparty/industrialbank/*`.
- [ ] **IB-2**: Скомпонувати банківський Shell і підключити `auth.app` (для автентифікації) та `editor.app` (для адміністрування менеджерами).
- [ ] **IB-3**: Запустити та верифікувати додаток на локальному раннері.

### 🤖 Крок 3. Наступні кроки для пайплайнів LLiMo.v3
- [ ] **LLM-1**: Реалізувати безперервний автономний TDD-цикл (автозапуск тестів та повернення помилок у промпт для самолікування коду).
- [ ] **LLM-2**: Впровадити стандарт збереження траси виконання (`session_trace.jsonl`) та утиліту відтворення (`playback`).

### 🧙‍♂️ Крок 4. Запуск глобального протоколу agents.md та оркестрації
- [ ] **AGN-1**: Запустити глобальний `agents.md` (Wizard Initialization) як базовий стандарт для всіх нових чатів на платформі.
- [x] **AGN-2**: Впровадити нативний `ReferenceValidator` у `@nan0web/db` для валідації посилань, в т.ч. мовних бар'єрів та зовнішніх URL (200 OK).
### 3. Завершення Next.js SSG Міграції додатка `apps/3rdparty/eaukraine.eu/web`
- [x] **Next.js App Router & SSG**: Успішно замінено старий Vite SPA на Next.js 16 (App Router).
- [x] **Динамічні та статичні сторінки**: Згенеровано статичні сторінки `/about`, `/mission`, `/team`, `/contacts`, `/privacy-policy`, `/digest`, `/categories/[category]` та динамічний роут `/[lang]/[category]/[slug]` з автоматичним фолбеком для статей (захист від 404).
- [x] **UI & UX полірування**:
  - Інтегровано шрифтовий стек `Roboto, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"`.
  - Реалізовано нескінченне завантаження новин (Infinite Scroll) через `IntersectionObserver`.
  - Усунено біле миготіння екрану (Anti-FOUC script у `<head>`).
  - Оновлено мобільну версію (`Footer.jsx` + `globals.css` без вилітів за межі `100vw`).
  - Додано кастомну 404 сторінку з підтримкою Dark Mode.
- [x] **Валідація збірки**: `npm run build` сформував усі 14 статичних сторінок без жодної помилки.

### 4. Payload CMS Content Migration — v0.3.0 (seed завершено, frontend migration триває)
- [x] **TS → JS сумісність**: Всі колекції конвертовано на чистий JS (.js розширення, без TS-анотацій `: CollectionConfig`, `as any`).
- [x] **payload.config.ts → .js**: Прибрано TypeScript-синтаксис, додано `.js` розширення в імпорти.
- [x] **Schema synchronization**: `migrate:fresh` → `generate:db-schema` → `migrate:create` → `migrate` — схема БД синхронізована з `.js` колекціями.
- [x] **Seed full import**: 3545 статей оброблено, 2804 створено/оновлено (2276 uk + 528 en locale records) за ~24 секунди.
- [x] **Idempotency verified**: Повторний запуск оновлює існуючі записи без дублікатів.
- [x] **externalId як ключ ідемпотентності**: Payload `id` (integer PK) не можна використовувати для text-ідентифікаторів.
- [x] **Process hang fix**: `db.destroy()` timeout + `process.exit(0)` для коректного завершення.
- [x] **Payload frontend adapter**: Створено `web/src/data/payload-api.js` з локалізованим читанням і мапінгом `article` у frontend shape.
- [x] **Server routes частково переведено**: головний новинний route, dashboard і category route використовують Payload adapter.
- [x] **Adapter tests**: Додано 3 unit-тести для locale, мапінгу та пошуку за `externalId`.
- [ ] Перевести slug-route, додати ISR/webhook і завершити багатомовний fallback для довільної кількості локалей.

---

## 🎯 Наступна сесія (Release v3.2.0)
У новому чаті необхідно провести загальну аналітику залишків по платформі `nan0web` та підготувати монорепозиторій до фінального релізу версії **3.2.0** (Крок 5).

_Документ оновлено Antigravity (АрхіТехноМаг) 27 липня 2026 року перед переходом у новий чат._
