declare namespace _default {
    /**
     * Allows declarative rendering in apps
     * and registers them programmatically if needed.
     */
    function bootstrap(): {
        'Auth.LoginForm': string;
    };
}
export default _default;
